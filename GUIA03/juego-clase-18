print("======== ejs pregunta 1 ========\n")
# iCual es la salida?
for i in range(5): 
    if i%2: print(i, end='')
    
# rta = 13
# Asi como estaba no sirve de nada el mas q para confundir y no mostrar el 1 y el 3 por separado 
# pero lo podes usar para espaciar cada valor ese end.

print("\n======== ejs pregunta 2 ========\n")
# iQué muestra este c6digo? 
total = 0
for i in range(1, 5): total += i

# extra - testing:
print(total) # rta = 10
